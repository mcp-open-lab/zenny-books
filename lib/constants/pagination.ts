/**
 * Pagination Constants
 * Default page sizes and limits for data fetching
 */

// Default page sizes
export const PAGE_SIZE_DEFAULT = 25;
export const PAGE_SIZE_MEDIUM = 50;
export const PAGE_SIZE_LARGE = 100;

// Similarity matching threshold (PostgreSQL pg_trgm)
export const SIMILARITY_THRESHOLD = 0.3;

// Confidence thresholds
export const MIN_CONFIDENCE_DEFAULT = 0.7;
export const MIN_CONFIDENCE_HIGH = 0.85;

